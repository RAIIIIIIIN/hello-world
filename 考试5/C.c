#include<stdio.h>
#include<string.h> 
int main()
{
	char num[5];

	int n;
	int i;
	int j;
	int len;
	scanf("%d",&n);
        for（len=1；len<7;len++）
	for(i=0;i<n;i++)
	{
		gets(num);
		lem=strlen(num);
		for(j=0;j<len;j++)
		{
			switch(num[j])
			{
				case 0:printf("*****\n***")
			}//这个程序没写完
		}
	}
}
