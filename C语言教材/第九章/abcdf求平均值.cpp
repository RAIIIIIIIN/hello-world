#include<stdio.h>
int main(){
	float compute_GPA(char grades[],int n);
	{int i;
	
	int grade[];
	int sum;
	
	 if(grades[i]='a')
	   grade[i]=4;
	 else if(grades[i]='b')
	   grade[i]=3;
	 else if(grades[i]='c')
	   grade[i]=2;
	 else if(grades[i]='d')
	   grade[i]=1;
	 else(grades[i]='f')
	   grade[i]=0;
	   
	 for(i=0;i<n;i++){
	 	sum+=grade[i];
	    return sum/n;
	 }    
	}
	printf("%d",compute_GPA({abcdf},5))
	
}
